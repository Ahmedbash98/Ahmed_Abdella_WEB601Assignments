import { FilterPlayersPositionPipe } from './filter-players-position.pipe';

describe('FilterPlayersPositionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPlayersPositionPipe();
    expect(pipe).toBeTruthy();
  });
});
